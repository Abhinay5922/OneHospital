# Environment Configuration
NODE_ENV=development
PORT=5000

# Database Configuration - Choose one:
# Option 1: MongoDB Atlas (Current - requires IP whitelisting)
MONGODB_URI=mongodb+srv://abhinaychauhn8055_db_user:OneHospital22@cluster0.zofyim3.mongodb.net/one_hospital?retryWrites=true&w=majority

# Option 2: Local MongoDB (Uncomment to use local database)
# MONGODB_URI=mongodb://localhost:27017/one_hospital

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_here_change_in_production
JWT_EXPIRE=7d

# CORS Configuration
CLIENT_URL=http://localhost:3000

# Socket.IO Configuration
SOCKET_PORT=5001
